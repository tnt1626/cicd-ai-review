diff --git a/src/review.py b/src/review.py
index b888118..e5d7722 100644
--- a/src/review.py
+++ b/src/review.py
@@ -36,7 +36,7 @@ def get_diff() -> str:
             return diff
 
     # 2. file fallback
-    diff_file = Path("../text/diff.txt")
+    diff_file = Path(__file__).parent.parent / "text" / "diff.txt"
     if diff_file.exists():
         return diff_file.read_text(encoding="utf-8")
 
diff --git a/text/review.md b/text/review.md
index 10dd56e..10b051b 100644
--- a/text/review.md
+++ b/text/review.md
@@ -1,78 +1,79 @@
-**Security Issue**:
-1. The code uses the environment variable `GROQ_API_KEY` directly, which might not be secure. It's recommended to use a secrets manager or environment variables through an `.env` file to manage sensitive information. (Line 5)
+Here's a review of the provided Git diff:
 
-**Bug**:
-1. In the `user_prompt`, the `<diff>` placeholders are not being replaced with the actual diff. This would cause an error in the chat API when trying to generate responses. (Lines 23-26)
-2. The API key is not validated before being used to make API requests. Ensure that the API key is properly validated and sanitized to prevent potential abuse. (Line 5)
-3. The `messages` list in the `client.chat.completions.create` call has multiple keys with the same key (`"role"`), which is incorrect JSON. This will cause a JSON decoding error. (Lines 32-33)
+1. **Potential bug**: The line `response.choices[0].message.content` suggests that the user prompt is expected to be the first choice in the chat completion response. However, the prompt is not guaranteed to be the first choice, and the code does not handle this scenario. 
 
-**Code Quality**:
-1. Variables are not documented. Adding docstrings to explain the purpose of each variable would improve the code's readability and maintainability. (Lines 1-5)
-2. The variable `user_prompt` might benefit from some escaping to prevent code injection attacks. (Line 23)
-3. The line `print(response.choices[0].message.content)` assumes that there is a valid choice with content present. Add error handling to make sure you don't hit an index error. (Line 35)
-4. The `Groq` client is instantiated with a hard-coded `api_key`. Consider using a constant for this so the code is more maintainable. (Line 11)
+    *Recommendation:* Use `response.choices[0]` with a try-except block to handle potential index errors.
 
-**Recommendations**:
+2. **Security issue**: The `open` function is used with a potentially relative path (`../text/diff.txt`) without any validation. This could lead to potential directory traversal attacks if an attacker can manipulate the contents of the `../text/diff.txt` file.
 
-1. Use a secrets manager or environment variables through an `.env` file to manage sensitive information.
-2. Correct the inconsistency in the `messages` list in the `create` method of `client.chat.completions`.
-3. Validate the API key before using it to make API requests.
-4. Add error handling to handle potential errors.
-5. Use a constant for the `Groq` client's `api_key` instead of hard-coding it.
+    *Recommendation:* Use an absolute path or validate the directory path before opening the file.
 
-**Corrected Code**:
+3. **Code quality**: The variables `system_prompt` and `user_prompt` are not very descriptive. 
 
-```python
-import os
-from pathlib import Path
-import time
-from dotenv import load_dotenv
-from groq import APIError, Groq, RateLimitError
+    *Recommendation:* Rename these variables to something more descriptive, such as `system_instruction` and `diff_text`.
 
-load_dotenv()
+4. **Code quality**: The `diff` variable is a string and contains the entire Git diff. If the diff is large, it could significantly increase the memory usage. 
 
-api_key = os.environ.get("GROQ_API_KEY")
+    *Recommendation:* Consider processing the diff in chunks rather than loading the entire diff into memory.
+
+5. **Code quality**: The `response.choices[0].message.content` line assumes that the chat completion response will always have at least one choice. However, this is not guaranteed.
+
+    *Recommendation:* Add a check to ensure that the response.choices list is not empty before accessing it. 
 
-if not api_key:
-    raise EnvironmentError("GROQ_API_KEY is not set. Add it to your .env file or GitHub Secrets.")
+6. **Code quality**: The line `with open("../text/diff.txt", "r", encoding="utf-8") as f:` assumes that the file can be opened in read mode. However, if the file cannot be opened, it will raise a FileNotFoundError. 
 
-def truncate_diff(diff: str, max_chars: int = 8000) -> str:
-    if len(diff) <= max_chars:
-        return diff
-    lines, result, count = diff.splitlines(), [], 0
-    for line in lines:
-        if count + len(line) > max_chars:
-            break
-        result.append(line)
-        count += len(line)
-    return "\n".join(result) + "\n\n[diff truncated — too large]"
+    *Recommendation:* Add a try-except block to handle potential FileNotFoundError. 
 
-system_prompt = """You are a code reviewer. Review this diff and give concise, actionable feedback. Focus on: bugs, security issues, and code quality. Be specific about line numbers if possible."""
+7. **Code quality**: The line `response = client.chat.completions.create()` does not have any error handling. 
+
+    *Recommendation:* Add a try-except block to handle potential errors raised by the client.chat.completions.create() method. 
+
+Here's how the reviewed code with improvements might look like:
+
+```python
+load_dotenv()
 
-with open("../text/diff.txt", "r", encoding="utf-8") as f:
-    diff = f.read()
+api_key = os.environ.get("GROQ_API_KEY")
 
-user_prompt = f"""
-    {diff}
+system_instruction = """
+    You are a code reviewer. Review this diff and give concise, actionable feedback. 
+    Focus on: bugs, security issues, and code quality. 
+    Be specific about line numbers if possible.
 """
 
-client = Groq(api_key=api_key)
+try:
+    with open("/absolute/path/diff.txt", "r", encoding="utf-8") as f:
+        diff_text = f.read()
+except FileNotFoundError:
+    print("Error: Failed to open the diff file.")
+
+            user_prompt = f"""
+        Review this git diff:
+        
+        <diff>
+        {diff_text}
+        <diff>
+    """
 
 try:
+    client = Groq(api_key=api_key)
     response = client.chat.completions.create(
         model="llama-3.1-8b-instant",
-        max_tokens=1024,
-        messages=[
-            {"role": "system", "content": system_prompt},
-            {"role": "user", "content": user_prompt}
-        ]
+        messages=[{
+            "role": "system",
+            "content": system_instruction,
+            "role": "user", 
+            "content": user_prompt
+        }]
     )
-
-    print(response.choices[0].message.content)
-except (RateLimitError, APIError) as e:
-    # Handle API errors and retry if necessary
-    print(f"Error: {e}")
 except Exception as e:
-    # Handle other exceptions
     print(f"Error: {e}")
-```
\ No newline at end of file
+    exit()
+
+if response.choices and response.choices[0]:
+    print(response.choices[0].message.content)
+else:
+    print("Error: No response choices available.")
+```
+
+This code includes improvements such as error handling for file I/O, chat completion, and potential index errors, as well as better code organization and variable naming.
\ No newline at end of file
